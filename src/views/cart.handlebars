<!DOCTYPE html>
<html>
<head>
    <title>Cart</title>
</head>
<body>
    <h1>Cart</h1>
    <ul>
    {{#each products}}
        <li>
            <h2>{{this.title}}</h2>
            <p>Description: {{this.description}}</p>
            <p>Price: ${{this.price}}</p>
            <p>Quantity: {{this.quantity}}</p>
            
        </li>
    {{/each}}
    </ul>
</body>
</html>
