<!-- views/productDetail.hbs -->
<!DOCTYPE html>
<html>
<head>
    <title>Product Detail</title>
</head>
<body>
    <h1>{{product.title}}</h1>
    <p>Description: {{product.description}}</p>
    <p>Price: ${{product.price}}</p>
    <button data-product-id="{{product._id}}" class="add-to-cart-btn">Add to Cart</button>
</body>
</html>
